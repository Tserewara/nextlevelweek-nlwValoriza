FROM node:14-alpine

ARG POSTGRES_PASSWORD

ARG MODE

ENV POSTGRES_PASSWORD=$POSTGRES_PASSWORD

ENV MODE=$MODE

WORKDIR nlwValoriza/

COPY . .

RUN yarn

RUN yarn tsc

EXPOSE 3000

ENTRYPOINT yarn start